@{
    ViewData["Title"] = "Chi ti?t don hàng";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Chi ti?t don hàng #@ViewBag.OrderId</h3>
    </div>

    <div class="card-body">

        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>S?n ph?m</th>
                    <th>S? lu?ng</th>
                    <th>Giá</th>
                    <th>Thành ti?n</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Áo so mi</td>
                    <td>2</td>
                    <td>500.000</td>
                    <td>1.000.000</td>
                    <td>
                        <a asp-action="EditCartItem" asp-route-id="@ViewBag.OrderId" asp-route-productId="1" class="btn btn-sm btn-warning">S?a</a>
                        <a asp-action="DeleteCartItem" asp-route-id="@ViewBag.OrderId" asp-route-productId="1" class="btn btn-sm btn-danger">Xóa</a>
                    </td>
                </tr>
            </tbody>
        </table>

        <hr />

        <a asp-action="Accept" asp-route-id="@ViewBag.OrderId" class="btn btn-success">Duy?t</a>
        <a asp-action="Shipping" asp-route-id="@ViewBag.OrderId" class="btn btn-info">Giao hàng</a>
        <a asp-action="Finish" asp-route-id="@ViewBag.OrderId" class="btn btn-primary">Hoàn t?t</a>
        <a asp-action="Reject" asp-route-id="@ViewBag.OrderId" class="btn btn-danger">T? ch?i</a>
        <a asp-action="Cancel" asp-route-id="@ViewBag.OrderId" class="btn btn-secondary">H?y</a>

    </div>
</div>
